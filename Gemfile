source :rubygems
gem 'redis'
gem 'redis-scripted', :require => "redis/scripted"

group :web do
  gem 'thin'
  gem 'sinatra'
  gem 'json'
  gem 'shotgun'
end

group :test do
  gem 'turn'
  gem 'minitest', '~> 2.7.0'
end

#gem 'em-redis', :git => "https://github.com/thoughtbot/em-redis.git"
